name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  build-and-test:
    runs-on: windows-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x
      - name: Restore
        run: dotnet restore MahjongAgent.slnx
      - name: Test
        run: dotnet test MahjongAgent.slnx --configuration Release --no-restore
      - name: Vulnerability audit
        run: dotnet list MahjongAgent.slnx package --vulnerable --include-transitive
