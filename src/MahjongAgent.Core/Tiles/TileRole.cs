namespace MahjongAgent.Core.Tiles;

/// <summary>
/// A dynamic role assigned by a round's rules. It never changes tile identity.
/// </summary>
public enum TileRole
{
  Indicator,
  Wildcard,
  Skin
}
