# 贡献指南

项目当前处于早期设计阶段。提交代码前请先通过 Issue 说明目标、规则来源和影响范围。

## 基本要求

- 领域核心不得引用 WPF、Win32 或其他 Windows 专属类型；
- 视觉观察不得绕过 `Observation Resolver` 直接修改 `GameState`；
- 新麻将规则必须包含来源、适用变体和测试样例；
- 不提交 API Key、完整用户截图、昵称、头像或聊天内容；
- 不加入进程注入、内存读取、内部协议拦截或自动出牌功能；
- 新的长期学习逻辑必须支持来源追踪、版本管理和回滚。

## 分支与提交

- 每个提交只处理一个清晰主题；
- 提交信息使用简洁的祈使句；
- UI 变更附带截图和对应状态说明；
- 规则变更附带测试用例和来源链接。
