# 14 主窗口 · 记忆与学习(高保真稿,新增)

> 对应 `review-codex.md` P0-4 与 `docs/backend-agent-memory.md`。产品核心 = 策略准确性、长期记忆、检索与自学习;本页是其主窗口载体。导航一级页面,位于"牌局复盘"与"规则与知识"之间。长期记忆管理**只在主窗口**;悬浮助手侧仅在详细策略面板显示"参考了哪些历史经验"(05 号文件 §3 第 ④ 层)。
> **v1.2 修订**(按 `review-codex-round-2.md`):R2-P1-2 记忆晋升加硬性门禁(验证未过禁用+原因;确认=人工复核,晋升由后端状态机执行并记录版本);回滚明确为发布操作;密度与通知策略按决定 #3/#5。

## 1. 页面框架

```text
┌─ 记忆与学习 ────────────────────────────────────────────────────────┐
│ ┌─ 健康卡带(全宽×4)────────────────────────────────────────────┐ │
│ │ confirmed 86 │ candidate 12(待审核3) │ rejected 9 · superseded 5 │ │
│ │ 最近整合:10 分钟前(局后自动)                                │ │
│ └──────────────────────────────────────────────────────────────────┘ │
│ ┌ 子导航 168 ──┐ ┌─ 内容区 ────────────────────────────────────────┐ │
│ │ 总览         │ │              (随子导航切换)                     │ │
│ │ 版本与发布   │ │                                                  │ │
│ │ 记忆检索     │ │                                                  │ │
│ │ 检索审计     │ │                                                  │ │
│ │ 待审核 (3)   │ │                                                  │ │
│ │ 情景记忆     │ │                                                  │ │
│ │ 后台学习队列 │ │                                                  │ │
│ │ 体检与维护   │ │                                                  │ │
│ └──────────────┘ └──────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────┘
```

健康卡带(4 卡,316×120):**confirmed** 计数(Success)/ **candidate** 计数(有待人审核时 Warning)/ **rejected · superseded** 计数(Neutral)/ **最近整合** 时间与触发方式(局后/空闲/手动);卡带常驻所有子页顶部。

**密度策略**(决定 #3):检索审计、情景记忆、后台学习队列等高密度表格子页默认**紧凑密度**;页面导航、健康卡带与待审核详情保持**舒适密度**。

**通知策略**(决定 #5):待审核 candidate 默认只出现在导航徽标与本页/仪表盘摘要,**不发托盘气泡**;未来可增加显式开关。

## 2. 子页规格

### 2.1 总览

- 记忆分层示意(只读图解):瞬时视觉(内存,不持久化)/ 本局工作记忆(GameEvent 溯源)/ 情景记忆(Episode)/ 语义记忆(已验证长期事实)/ 程序记忆(代码发布,Agent 无权在线修改);
- 各类语义记忆计数:窗口视觉映射、已验证战术模式、对手行为统计、用户提示偏好、典型牌局;
- 待办列表:待审核 candidate、记忆冲突、队列失败重试项——每项可点击直达对应子页。

### 2.2 版本与发布(**不是只读页**,R2-P1-2)

| 行 | 内容 |
|---|---|
| 策略版本 | `0.1.0`(当前)· 影子策略 `0.2.0-cand` 评测中(固定评测集进度 62%) |
| 规则档案 | `wuhan-koukoufan v3`(本档案已确认 4 项 · 待确认 5 项 · 冲突 1 项)→ 跳规则与知识页 |
| 提示模板 | `prompt-v3`;模型 `mock-v1`(Provider MockProvider) |
| 发布记录 | 版本、时间、评测指标变化、晋升依据(固定评测集提升且无规则回归)、[回滚] |

- 晋升走"影子模式 → 固定评测集 → 发布"流程,UI 不提供跳过评测的操作(backend §11–12);
- **回滚属于发布操作**:二次确认 + 展示影响范围(引用该版本的档案数、策略、记忆条目计数)+ 写审计记录;回滚生成新版本记录,不删除历史版本链。

### 2.3 记忆检索(MemorySearch,紧凑密度)

```text
┌─ 检索 ──────────────────────────────────────────────────────────┐
│ 规则集 [wuhan-koukoufan ▾] 巡目 [8] 向听 [1] 癞子 [1] 开口 [2]  │
│ 对手模式 [疑似清一色 ▾]  文本 [对家疑似清一色时如何兼顾牌效____] │
│ [检索]                                                           │
│ ── 结果(带命中理由)────────────────────────────────────────── │
│ ▸ Episode #0831 · 同档案第3局第9巡   综合 0.87                  │
│   结构相似 0.91 · 语义 0.78 · 关键词 0.66 · 时效 1.0 · 质量 0.9  │
│   当时建议 首选 9s · 实际 打7z · [查看该巡复盘]                 │
│ ▸ Episode #0402 · …                                            │
└──────────────────────────────────────────────────────────────────┘
```

- 结构化条件为主(规则集精确过滤 → 阶段/结构特征 → 向听/癞子/开口/对手模式),文本检索为辅(FTS5/语义);**牌局结构相似度优先于自然语言相似度**(backend §9);
- 每条结果必须展示**命中理由**:分项得分(domain/semantic/keyword/relation/temporal/quality − 冲突/低置信惩罚),不允许只给一个无解释的相关度。

### 2.4 检索审计(Retrieval Audit,紧凑密度)

每次建议的检索留痕(backend:`retrieval_audit` 表):

```text
┌─ 检索审计 ──────────────────────────────────────────────────────┐
│ 列表:#231 · 第6巡 · 首选 打九条 · 命中 2 · 耗时 34ms   [筛选▾] │
│ ── 详情(#231,从详细策略面板"参考经验"跳入时定位到此)───────── │
│ 查询:ruleset=wuhan-koukoufan · turn=6 · shanten=1 · wildcard=1 │
│ 候选 3:                                                        │
│  ① Episode #0831 0.87(domain .91/semantic .78/kw .66/relation  │
│    .70/temporal 1.0/quality .90 − conflict 0 − lowconf .05)    │
│  ② Episode #0402 0.74(…)  ③ Episode #0118 0.61(已弃:冲突惩罚)│
│ 最终注入 Agent:① ②(摘要 210 字)[查看注入内容]                │
└──────────────────────────────────────────────────────────────────┘
```

### 2.5 待审核(MemoryReview,详情舒适密度;晋升硬性门禁 R2-P1-2)

candidate 记忆审核队列(复核 backend 三态 + superseded):

| 列 | 说明 |
|---|---|
| 内容 | 候选经验摘要(如"对家连续摸后即打时筒子中张危险上调") |
| 来源 | 溯源链:Episode → GameEvent ID 列表(可点开) |
| 适用范围 | 适用 RuleProfile + 有效时间 valid_from/valid_to |
| 验证状态 | 规则验证 ✓/✗/未运行 · 冲突处理 无/未决 · 策略回放结论 · 固定评测集结果 |
| 操作 | [确认] [拒绝] |

**确认按钮的硬性门禁**(R2-P1-2):

- 规则验证未通过、存在未处理冲突、固定评测未通过或未运行时,`确认`**禁用并逐条显示原因**(如 `规则验证未运行 — 需先在后台队列完成`);
- `确认` 只表示**人工复核通过**;最终晋升由后端状态机执行并写入版本(candidate → confirmed 不是 UI 一键直达),晋升结果回读显示;
- `拒绝` 需选择原因,确认/拒绝均写审计记录;
- 冲突项并排对照(旧 confirmed vs 新 candidate),确认新项时旧项自动转 `superseded`,版本链保留,可回滚(按 §2.2 发布操作)。

### 2.6 情景记忆(Episodes,紧凑密度)

按局浏览 Episode 列表:局/巡、特征(向听、有效进张、癞子数、开口数、下家吃牌风险)、动作语义(首选/备选/不建议)与实际动作、策略版本;固定提示 `Episode 是经历,不自动代表正确经验;单局输赢不作为策略标签`;[查看该巡复盘] 跳 09 号文件页。

### 2.7 后台学习队列(紧凑密度)

`consolidation_jobs` 列表:触发原因(局后/空闲/累积/冲突/手动)、状态(排队/运行/完成/失败重试)、产出(candidate 数、冲突数);任务幂等可重试(backend §15);[立即整理] 手动触发(空闲时执行,不阻塞牌局提示)。

### 2.8 体检与维护(危险区)

- 去重(相似记忆合并预览 → 确认)、记忆体检(失效/低质量/过期项清单)、按 namespace 删除、版本回滚(按 §2.2 发布操作);
- 全部 Danger 操作:二次确认 + 影响范围清单 + 审计记录;confirmed 记忆删除不级联删除原始 Episode 与 GameEvent。

## 3. 与全局的关系

- 详细策略面板"参考经验"(05 §3 ④)与本页 `检索审计` 详情一一对应(#231 示例);复盘页(09)Episode 与本页 `情景记忆` 同源;
- 本页所有记忆条目必含:来源、适用范围(RuleProfile)、有效时间、可信度、版本(backend §8.3 MemoryRecord 字段);
- 自动形成 candidate 的范围与禁止自动晋升的范围(backend §12)在本页"总览"以只读说明呈现;程序记忆(规则/牌效算法/策略权重/提示模板)经 Git 发布,UI 永远只读;
- 通知策略:待审核仅以导航徽标 + 本页/仪表盘摘要呈现,不发托盘气泡(决定 #5)。

## 4. 状态覆盖

| 状态 | 呈现 |
|---|---|
| 空(新装机,无记忆) | 空态:`还没有长期记忆 · 完成牌局后自动开始积累` + 分层图解 |
| 整合中 | 卡带显示 `后台整合中…`(静态,不打断浏览);队列子页进度条 |
| 待审核积压 | 子导航计数徽标;卡带 candidate 转 Warning |
| 冲突 | 待审核子页置顶冲突区,Danger chip + 对照视图 |
| 验证未运行/未过 | 待审核确认按钮禁用 + 原因清单(R2-P1-2) |
| 无网络 | **本地记忆全部可用**(检索/审核/维护不受影响);仅 Embedding/LLM 提炼类后台任务排队等待,注明原因 |
| 删除确认 | 模态列出范围与不可恢复项,输入命名空间确认 |
